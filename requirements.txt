# YOLOv8x + ByteTrack on MOT17 — dependencies
#
# Install with: pip install -r requirements.txt
#
# Note: if running in Google Colab, use setup.py instead of this file
# directly — Colab requires a runtime restart after fixing the numpy
# environment before installing these packages.

ultralytics>=8.0.0
scipy>=1.10.0
matplotlib>=3.7.0
kaggle>=1.5.0
tqdm>=4.65.0
torch>=2.0.0
torchvision>=0.15.0

# TrackEval is installed via git clone in setup.py, not pip:
# git clone https://github.com/JonathonLuiten/TrackEval.git
